package high.extend;

/**
 *
 */

/**
 * @author internous
 *
 */
public class Main {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
	// TODO 自動生成されたメソッド・スタブ
		PoisonMatango m = new PoisonMatango('a');
		Hero h = new Hero("aaaa");
		m.attack(h);
		m.attack(h);
		m.attack(h);
		m.attack(h);
		m.attack(h);
		m.attack(h);

	}
}
